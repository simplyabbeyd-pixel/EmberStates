# EmberStates

This repository is the recovery and organization hub for the **Emberstates** worldbuilding project.

## Recovery package

- `AGENTS.md` - operating rules for future Codex/GitHub recovery passes
- `docs/INDEX.md` - cross-surface documentation and source-anchor index
- `recovery/README.md` - provenance, scope, current evidence, and known limitations
- `recovery/EMBERSTATES_REVIEW__2026-08-02.md` - Drive-grounded review ruling for the compiled packet
- `recovery/Emberstates_Master_Recovery_Index.xlsx` - six-sheet source and recovery index

## Current Drive anchor

The current Drive source surface is the shared `emberstates` folder:

`https://drive.google.com/drive/folders/1Ftz1SiQxImAbcLj2a7XlKNzhSUtPuoxZ`

Inside it, `01_PROJECT_HUB` contains the richer current workbook `Emberstates_Master_Recovery_Index_v2.xlsx` and the review note mirrored here.

A separate private/root Drive folder named `emberstates` contains only `agent-ember-states-*.json` files. Treat that as an agent-state export packet, not the main recovery hub.

## Integrity rules

- Inventory presence does not automatically establish canon.
- Project hubs, spreadsheet indexes, bookmark IDs, and image filenames are provenance leads, not source authority by themselves.
- Missing URLs, identifiers, ZIP binaries, manifests, schemas, entity lists, location lists, and source chat exports remain explicitly unresolved until fetched/read.
- Historical ChatGPT sandbox ZIPs cannot be reconstructed from filenames alone.
- Core Emberstates material, Emberstates-related material, Hells Branch/runtime material, and connected external material must remain separately classified until verified.
- Hells Branch, JanitorAI-adjacent, generated-play, adult/intensity, or runtime material must not be promoted into main Triquel canon without explicit approval.

## Current ruling

This repo is a recovery/control map, not canon proof.

Use it to drive source inventory and verification. Promote only rows with readable source evidence, authority tier, canon status, and explicit approval/readback.

## Maintenance note

The GitHub workbook artifact should be replaced with the Drive/local v2 workbook when binary file update support is available. A prior read-only check found the repo workbook download had ZIP central-directory errors, while the Drive/local v2 workbook opened cleanly.